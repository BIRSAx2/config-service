spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BIRSAx2/trafalgar-config.git
          default-label: main
server:
  port: 8888
